# CueCast 기능 명세서

## 1. 문서 개요

이 문서는 CueCast의 사용자 기능, 서버 기능, 데이터 수집 기능과 검증 조건을 정의합니다. 구현 기준은 `main` 브랜치의 `YOLO/local_probability_server.py`, `YOLO/cuecast_yolo/`, `src/`, `db/`, `deploy/`입니다.

### 1.1 시스템 목표

CueCast는 다음 세 가지 확률을 하나의 서비스에서 제공합니다.

1. **경기 전 승률:** 선수 누적 기록과 2026 시즌 시작 스냅샷 기반
2. **현재 샷 성공률:** 샷 직전 세 공의 정규화 좌표 기반
3. **실시간 세트 승률:** 경기 전 우위, 현재 점수, 공격 선수와 현재 샷 성공률 기반

### 1.2 주요 사용자

| 사용자 | 목적 |
|---|---|
| 일반 시청자 | 경기 전·경기 중 우위와 샷 난이도 확인 |
| 시연자 | YouTube 영상과 실시간 분석 결과를 함께 발표 |
| 데이터 관리자 | 영상 URL을 큐에 등록하고 샷 데이터를 축적 |
| 개발자 | API, 모델, OCR, DB 적재 상태 점검 |

### 1.3 용어

| 용어 | 정의 |
|---|---|
| 배치 / 포메이션 | 샷 직전 흰 공·노란 공·빨간 공의 위치 |
| 수구 | 현재 공격 선수가 치는 흰 공 또는 노란 공 |
| 정지 배치 | 세 공이 설정 시간 동안 허용 오차 이내에서 움직이지 않은 상태 |
| 턴 | 점수판 이닝 이벤트를 기준으로 확정한 한 번의 공격 구간 |
| 경기 전 승률 | 경기 시작 전 선수 데이터만으로 계산한 승리 확률 |
| 실시간 세트 승률 | 현재 세트의 점수·수구·샷 성공률을 반영한 승리 확률 |
| 성공 라벨 | 점수판 점수 상승으로 확인된 3쿠션 득점 여부 |

---

## 2. 기능 요구사항

### 2.1 시스템 실행 및 상태

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| SYS-01 | 로컬 서버 실행 | 사용자는 루트의 `run_cuecast_local.sh`로 UI와 API를 실행할 수 있어야 한다. | `127.0.0.1:8765` 접속 및 `/api/v1/health` 200 응답 | 필수 |
| SYS-02 | 데이터 없는 실행 | RDS 또는 export 연결이 실패해도 가능한 기능은 계속 실행되어야 한다. | 서버가 종료되지 않고 비활성 기능 안내 표시 | 필수 |
| SYS-03 | 상태 확인 | 서버는 샷 데이터 수, 모델 버전과 경기 전 데이터 소스를 반환해야 한다. | health 응답에 `records`, `modelVersion`, `prematchSource` 포함 | 필수 |
| SYS-04 | OCR 실행 프로그램 확인 | 점수판 OCR 전에 운영체제에 설치된 Tesseract 실행 파일을 사용할 수 있어야 한다. | `tesseract --version` 성공 및 실행 경로 인식 | 필수 |
| SYS-05 | 통합 실행 범위 | YouTube 영상 분석과 DB 연동을 포함한 전체 기능은 현재 로컬 서버와 SSH 터널 방식으로 제공해야 한다. | 로컬 실행에서 영상·DB·OCR·확률 화면 동작 | 필수 |

### 2.2 선수 선택 및 경기 전 승률

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| PRE-01 | 리그 선택 | PBA와 LPBA 선수 목록을 분리해 조회해야 한다. | `league=PBA|LPBA` 필터 적용 | 필수 |
| PRE-02 | 선수 검색 | 선수명과 축약명을 이용해 두 선수를 선택할 수 있어야 한다. | 검색 결과 선택 시 내부적으로 정식 선수명이 저장됨 | 필수 |
| PRE-03 | 동일 선수 방지 | 같은 선수를 A와 B에 동시에 선택할 수 없어야 한다. | 422 또는 사용자 오류 안내 | 필수 |
| PRE-04 | 경기 전 승률 계산 | Elo·통산·시즌·최근·세부 경기력을 결합해야 한다. | A/B 확률 합이 1이고 입력 순서 교환 시 대칭성 유지 | 필수 |
| PRE-05 | 신뢰도 재가중 | 데이터가 부족한 항목은 기본 가중치에 신뢰도를 곱해 재정규화해야 한다. | `componentConfidences`, `finalWeights` 반환 | 필수 |
| PRE-06 | 설명 제공 | 상위 영향 요인과 신뢰도 수준을 제공해야 한다. | `keyFactors`, `confidence`, `formulaNote` 반환 | 선택 |
| PRE-07 | 선수 이미지 | DB에 이미지가 있으면 반환하고, 없으면 UI 대체 표시를 사용해야 한다. | 이미지 200 또는 `image_not_found` 404 | 선택 |
| PRE-08 | 상대 전적 표시 | 상대 전적은 참고 정보로 표시하되 최종 승률에는 반영하지 않는다. | `headToHeadIncludedInProbability=false` | 선택 |

### 2.3 YouTube 영상 연결

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| VID-01 | URL 분석 | YouTube URL에서 제목, 영상 길이와 소스 종류를 조회해야 한다. | `/api/v1/youtube/info` 정상 응답 | 필수 |
| VID-02 | 시점 분석 | 사용자가 지정한 시점 이전 구간에서 마지막 안정 배치를 찾을 수 있어야 한다. | timestamp와 lookback을 입력해 배치·확률 반환 | 선택 |
| VID-03 | 실시간 시작 | 현재 재생 시점부터 영상 분석 워커를 시작해야 한다. | 상태가 `starting` 또는 `running`으로 변경 | 필수 |
| VID-04 | 정지 | 사용자가 분석 워커를 중지할 수 있어야 한다. | 추가 프레임 분석이 중단되고 상태 반환 | 필수 |
| VID-05 | 시점 동기화 | 브라우저 재생 위치 변화에 맞춰 분석 워커를 이동시킬 수 있어야 한다. | `timestamp_seconds` 반영 | 필수 |
| VID-06 | 수구 수동 지정 | OCR 확정 전 또는 테스트 시 수구를 수동 변경할 수 있어야 한다. | `white` 또는 `yellow`만 허용 | 선택 |
| VID-07 | 접근 거부 처리 | 배포 환경 또는 외부 네트워크에서 YouTube 요청이 거부되면 서버가 원인을 오류 상태로 전달해야 한다. | 워커 종료 대신 `error` 상태와 상세 메시지 반환 | 필수 |

### 2.4 Computer Vision 및 점수판 OCR

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| CV-01 | 탑뷰 검증 | 사선·클로즈업·인트로 화면은 분석 대상에서 제외해야 한다. | plausible top-view 조건 실패 시 배치 확정 안 함 | 필수 |
| CV-02 | 당구대 검출 | 플레이 가능한 당구대 안쪽 영역을 찾고 좌표 변환 기준을 생성해야 한다. | 네 모서리와 변환 행렬 확보 | 필수 |
| CV-03 | 공 검출 | 흰 공·노란 공·빨간 공 세 개를 각각 식별해야 한다. | 세 색상 좌표가 모두 존재 | 필수 |
| CV-04 | 좌표 정규화 | 공 좌표를 당구대 기준 `[0,1] × [0,1]`로 변환해야 한다. | 범위 밖 좌표는 폐기 | 필수 |
| CV-05 | 정지 배치 | 세 공이 설정 시간 동안 안정적일 때만 배치를 확정해야 한다. | `confirmed=true`, `layoutSource=stopped` | 필수 |
| CV-06 | 중복 제거 | 직전 확정 배치와 차이가 작은 이벤트는 중복 기록하지 않아야 한다. | 중복 임계값 이하 이벤트 제외 | 필수 |
| OCR-01 | 점수 인식 | 두 선수의 현재 점수와 연속 득점 값을 읽어야 한다. | 부분 인식 결과를 이전 값과 병합 | 필수 |
| OCR-02 | 수구 인식 | 점수판의 현재 이닝 표시 색을 통해 수구를 결정해야 한다. | `activeColor=white|yellow` | 필수 |
| OCR-03 | 성공 판정 | 점수 상승을 성공 라벨의 유일한 기준으로 사용해야 한다. | `success_method=scoreboard` 데이터만 적재 | 필수 |
| OCR-04 | 점수 초기화 | 세트 전환 또는 OCR 오류 시 점수 필드만 초기화할 수 있어야 한다. | 선수 선택과 서버는 유지, 점수·런은 null | 선택 |
| OCR-05 | Tesseract 의존성 오류 | Tesseract가 설치되지 않았거나 `PATH`에서 찾을 수 없으면 OCR 불가 상태를 명확히 안내해야 한다. | 설치 명령과 확인 방법을 포함한 오류 로그·문서 제공 | 필수 |

### 2.5 샷 성공률

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| SHOT-01 | 입력 검증 | `before`에 세 공 좌표와 `shooter`가 있어야 한다. | 누락·형식 오류 시 400 | 필수 |
| SHOT-02 | 역할 정규화 | 수구와 두 목적구 역할을 입력 색상에 맞게 변환해야 한다. | 응답에 `roles` 포함 | 필수 |
| SHOT-03 | 모델 선택 | 저장된 CatBoost 모델, Logistic 모델 또는 Bootstrap 순으로 사용 가능해야 한다. | health에 실제 모델 버전 표시 | 필수 |
| SHOT-04 | 하이브리드 계산 | 좌표 모델, 유사 배치와 Grid 결과를 데이터 양에 따라 결합해야 한다. | `successProbability`와 구성 요소 반환 | 필수 |
| SHOT-05 | 좌표 오차 반영 | 위치 오차 범위의 반복 예측으로 민감도와 신뢰도를 계산해야 한다. | `position_error_mm` 입력 반영 | 선택 |
| SHOT-06 | 설명 제공 | 난이도, 신뢰도와 주요 이유를 UI에 표시해야 한다. | 사용자에게 확률 외 설명 노출 | 선택 |

### 2.6 실시간 세트 승률

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| LIVE-01 | 선수 확정 | 실시간 계산 전에 두 선수의 정식 DB 이름이 확정되어야 한다. | 이름 미확정 시 waiting 상태 | 필수 |
| LIVE-02 | 경기 전 미리보기 | 점수판과 공 배치가 없어도 선수 선택 후 경기 전 승률을 표시해야 한다. | waiting 응답의 `prematch` 존재 | 필수 |
| LIVE-03 | 현재 점수 반영 | 두 선수의 현재 세트 점수를 입력으로 사용해야 한다. | 점수 미인식 시 계산 대기 | 필수 |
| LIVE-04 | 공격권 반영 | 현재 수구 색을 선수와 매핑해 공격 선수를 결정해야 한다. | `activeColor` 변경 시 재계산 | 필수 |
| LIVE-05 | 현재 샷 반영 | 확정된 현재 배치의 성공률을 실시간 세트 승률에 반영해야 한다. | 새 confirmedVersion에서 재계산 | 필수 |
| LIVE-06 | 범위 명시 | 응답과 UI는 전체 경기 승률이 아닌 현재 세트 승률임을 명시해야 한다. | `probabilityScope=current_set` | 필수 |
| LIVE-07 | 캐시 무효화 | 점수 또는 공격 선수가 바뀌면 이전 배치 확률을 그대로 재사용하지 않아야 한다. | 상태가 재계산 대기로 전환 | 필수 |

### 2.7 UI 및 기록

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| UI-01 | 실시간 분석 화면 | 영상, 점수판, 샷 성공률, 버추얼 당구대와 세트 승률을 한 화면에 표시 | 주요 영역이 동시에 갱신 | 필수 |
| UI-02 | 로딩·오류 상태 | 데이터 연결 대기, 분석 중, 재계산, 오류를 구분해 표시 | 상태별 문구와 tone 적용 | 필수 |
| UI-03 | 샷 기록 | 확정된 정지 배치만 시간순 기록에 추가 | 잠정·이동 중 배치 제외 | 선택 |
| UI-04 | 선수 통계 | 선수 전적과 세부 경기력 지표를 조회 | 선수 선택 시 카드 갱신 | 선택 |
| UI-05 | 반응형 | 작은 화면에서도 점수판과 주요 확률을 읽을 수 있어야 한다. | 모바일 폭에서 1열 또는 축약 레이아웃 | 선택 |
| EXT-01 | 확장 프로그램 | 브라우저 사이드패널에서 서버 최신 결과를 조회 | extension preview 또는 설치 후 표시 | 선택 |

### 2.8 배치 데이터 수집 및 저장

| ID | 기능 | 요구사항 | 완료 조건 | 우선순위 |
|---|---|---|---|---|
| PIPE-01 | 작업 큐 | `jobs/pending/*.txt`의 URL을 순차 처리해야 한다. | done/failed 폴더 이동 및 이력 기록 | 필수 |
| PIPE-02 | 중복 방지 | 이미 로컬 또는 S3에 결과가 있는 영상은 건너뛰어야 한다. | 동일 URL 재입력 시 skip | 필수 |
| PIPE-03 | 영상 다운로드 | yt-dlp와 JS challenge 대응 옵션으로 영상을 내려받아야 한다. | 다운로드 실패 사유 기록 | 필수 |
| PIPE-04 | 턴 추출 | 점수판 이닝 이벤트를 기준으로 턴을 생성하고 좌표를 결합해야 한다. | `turns.jsonl`, `turns.csv` 생성 | 필수 |
| PIPE-05 | 품질 필터 | 점수판 판정 불가 또는 좌표 범위 밖 턴은 DB 학습 데이터에서 제외 | loader 이중 필터 | 필수 |
| PIPE-06 | S3 업로드 | 영상별 결과를 정해진 prefix에 업로드 | `results/<video_id>/turns.jsonl` 존재 | 필수 |
| DB-01 | RDS upsert | EC2 loader가 `(video_id, turn)` 기준으로 중복 없이 적재 | 재실행해도 행 중복 없음 | 필수 |
| DB-02 | export | RDS 샷 데이터를 모델 로드용 JSONL로 내보내야 한다. | 최신 export 행 수 확인 가능 | 필수 |

---

## 3. 주요 사용자 흐름

### 3.1 경기 전 승률 조회

1. 사용자가 PBA 또는 LPBA 리그를 선택합니다.
2. 서버가 활성 선수 목록을 반환합니다.
3. 사용자가 서로 다른 두 선수를 선택합니다.
4. 서버가 최신 선수 스냅샷과 참고 상대 전적을 조회합니다.
5. 다섯 개 구성 확률과 신뢰도를 계산합니다.
6. UI가 최종 승률, 주요 요인, 선수 전적과 이미지를 표시합니다.

### 3.2 실시간 영상 분석

1. 사용자가 로컬 CueCast 서버를 실행하고 Tesseract·FFmpeg 설치 상태를 확인합니다.
2. 사용자가 YouTube URL을 입력합니다.
3. 서버가 영상 제목과 길이를 확인합니다.
4. 사용자가 두 선수를 DB 목록에서 확정합니다.
5. 분석 워커가 재생 시점에서 프레임 처리를 시작합니다.
6. Tesseract 기반 점수판 OCR은 점수와 수구를 갱신합니다.
7. 공 검출기는 세 공이 정지한 배치를 확정합니다.
8. 샷 성공률 엔진이 현재 포메이션을 계산합니다.
9. 실시간 세트 승률 엔진이 선수·점수·공격권·샷 성공률을 결합합니다.
10. 브라우저가 최신 상태를 polling하여 화면을 갱신합니다.

### 3.3 학습 데이터 수집

1. 관리자가 URL을 `jobs/pending/`에 등록합니다.
2. `process_queue.py`가 다운로드와 턴 추출을 수행합니다.
3. 결과를 로컬과 S3에 저장합니다.
4. EC2 cron이 S3 결과를 검사합니다.
5. 점수판 라벨과 유효 좌표가 있는 턴만 RDS에 upsert합니다.
6. export cron이 모델용 JSONL을 생성합니다.
7. 서버 재시작 시 최신 export를 읽어 모델을 로드하거나 학습합니다.

---

## 4. 데이터 검증 규칙

- `shooter`는 서비스 입력에서 `white` 또는 `yellow`만 허용합니다.
- 세 공 좌표는 모두 숫자 두 개의 배열이며 각 값은 `0.0 ≤ value ≤ 1.0`이어야 합니다.
- 같은 선수 코드를 양쪽에 사용할 수 없습니다.
- PBA와 LPBA 선수를 교차 계산하지 않습니다.
- 경기 전 A/B 승률 합은 부동소수 오차 범위에서 1이어야 합니다.
- 점수판으로 성공 여부를 확인하지 못한 턴은 학습·DB 적재에서 제외합니다.
- 실시간 세트 승률은 점수, 수구와 확정 샷 확률이 모두 준비된 경우에만 ready 상태가 됩니다.
- 영상 분석 중 새 점수 또는 공격권이 감지되면 이전 배치 확률은 무효화합니다.

---

## 5. 비기능 요구사항

| ID | 구분 | 요구사항 |
|---|---|---|
| NFR-01 | 안정성 | DB·SSH·S3 일부 연결 실패가 전체 로컬 UI 종료로 이어지지 않아야 한다. |
| NFR-02 | 데이터 무결성 | DB 적재는 자연키 또는 복합 PK 기반 upsert로 중복을 막아야 한다. |
| NFR-03 | 보안 | DB 비밀번호, AWS 키, 쿠키와 service credential을 Git에 저장하지 않는다. |
| NFR-04 | 설명 가능성 | 최종 확률뿐 아니라 구성 요소, 신뢰도, 난이도 또는 주요 이유를 제공한다. |
| NFR-05 | 재현성 | 모델 버전, 데이터 레코드 수와 입력 좌표를 응답 또는 로그로 확인할 수 있어야 한다. |
| NFR-06 | 성능 | polling API는 장시간 연결 없이 짧은 JSON 응답을 반환하고 캐시를 사용하지 않는다. |
| NFR-07 | 호환성 | OpenCV 5 미만, Python 3.11/3.12 환경을 기본 지원한다. |
| NFR-08 | 접근성 | 주요 입력과 버튼은 키보드 포커스와 상태 텍스트를 제공한다. |
| NFR-09 | 배포 가능 범위 | 원격 배포에서 접근 거부가 해결되기 전까지 전체 통합 기능의 공식 실행 환경은 로컬로 한정한다. |
| NFR-10 | 시스템 의존성 | Tesseract와 FFmpeg의 설치 여부를 실행 전 확인하고 누락 시 즉시 식별 가능한 오류를 제공한다. |

---

## 6. 현재 한계와 제외 범위

- 방송 전체에서 임의의 카메라 구도와 점수판 템플릿을 완전히 자동 지원하지 않습니다.
- 공의 회전, 큐 속도, 타격점과 실제 물리 궤적을 직접 측정하지 않습니다.
- 실시간 화면은 현재 세트 승률이며 세트 누적을 포함한 전체 경기 승률은 후속 범위입니다.
- 점수판이 없는 영상은 성공 라벨 데이터셋을 만들 수 없습니다.
- 모델 결과는 분석·시연 목적이며 베팅 또는 금전적 의사결정 용도가 아닙니다.
- 사용자 계정, 개인화, 다중 사용자 세션과 권한 관리는 현재 범위에 포함하지 않습니다.
- 배포 환경에서 YouTube URL 분석과 DB 연동을 동시에 수행할 때 접근 거부 오류가 발생해, 현재 전체 기능은 로컬 실행과 SSH 터널 연결을 기준으로 합니다.
- Tesseract는 Python 패키지 설치와 별도로 운영체제에 설치해야 하며, 실행 파일이 없으면 점수판 OCR과 이를 사용하는 실시간 계산을 수행할 수 없습니다.

---

## 7. 관련 문서

- [README](../README.md)
- [API 명세서](api-spec.md)
- [DB 스키마 문서](database-schema.md)
- [화면 설계서](screen-design.md)
- [배포 가이드](DEPLOYMENT.md)
- [데이터 계약](DATA_CONTRACT.md)
- [턴 추출 판정 기준](EXTRACTION_CRITERIA.md)
- [경기 전 승률 계산](PREMATCH_PROBABILITY.md)
- [샷 성공률 모델](../YOLO/PROBABILITY_MODEL.md)
